# 核心依赖
openai>=1.0.0
spacy>=3.4.0
tqdm>=4.65.0
neo4j>=5.0.0

# 知识图谱补全（KGC）依赖
torch>=2.0.0
torch-geometric>=2.3.0
numpy>=1.24.0

# 可选依赖（用于PDF处理）
# PyPDF2>=3.0.0  # 如果需要PDF处理功能，取消注释

# 开发依赖（可选）
# pytest>=7.0.0  # 用于运行测试
